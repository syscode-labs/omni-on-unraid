# Secrets

This repository is currently operated manually (no CI apply path).

No repository secrets are required for normal operation.

If a CI deployment path is reintroduced later, add secrets via a dedicated SOP first.
