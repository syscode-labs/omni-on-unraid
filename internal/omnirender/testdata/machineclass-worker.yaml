---
metadata:
  namespace: default
  type: MachineClasses.omni.sidero.dev
  id: unraid-worker
spec:
  autoprovision:
    providerid: libvirt
    providerdata: |
      cores: 3
      memory: 4096
      disk_size: 40
      storage_pool: "omni-domains"
      network_interfaces:
        - driver: "virtio"
          network_name: "default"
